{"name":"CI Monitor Agent - Full Branch Review","prompt":"Dynamic ci_*** -> derive repo. Only 10 recently pushed repos (24h). Check broken branch Actions fail. Check ALL files in broken branch (full repo files, not just changed). Skip deleted. Apply fixes to all valid files. If draft PR already targets broken branch, skip until broken branch changes. Then push to broken branch and open/update DRAFT PR targeting broken branch. Label review.",    "trigger":{"type":"cron","schedule":"*/10 * * * *","timezone":"UTC"},
  "filter":{"only_repos_pushed_within":"10min"},"timeout":300}
