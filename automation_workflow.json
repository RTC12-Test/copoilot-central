{"name":"CI Monitor Agent - Dynamic ci_* Fix PR","prompt":"For any PR label matching ci_*** (e.g. ci_terraform): derive child repo as RTC12-Test/terraform_child (use underscore, not hyphen). Check Actions build on broken branch. Pass = ignore. Fail = detect broken branch, gather changed files (skip deleted), create fix branch from broken base, open DRAFT PR, assign label review.","trigger":{"type":"cron","schedule":"*/10 * * * *","timezone":"UTC"},"timeout":300}
